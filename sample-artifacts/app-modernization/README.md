# App Modernization — Legacy Web App to Azure

Sample artifact pack for AZ Prototype demos and workshops.

Use with:

```bash
mkdir -p requirements
cp sample-artifacts/app-modernization/* ./requirements/
az prototype design --artifacts ./requirements --context "Use these materials to create a pragmatic Azure prototype design."
```
